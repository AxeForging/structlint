run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - misspell
    - errcheck
    - gofumpt
  disable:
    - depguard
issues:
  exclude-use-default: false
